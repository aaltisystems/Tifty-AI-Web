/**
 * Tifty AI — Client-Side Environment Configuration (EXAMPLE)
 * --------------------------------------------------
 * Copy this file as env.js and replace with your real values.
 *
 * ⚠️  NEVER commit the real env.js to public repositories.
 *     Make sure env.js is included in your .gitignore.
 */
window.__ENV__ = {
    WEBHOOK_URL: "https://your-webhook-url.example.com/audit"
};
